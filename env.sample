# 複製此檔為 .env 並依環境修改
# cp .env.sample .env

# 服務埠
PORT=3000

# 測試目標 API：
TARGET_BASE_URL=http://127.0.0.1:5050

# 階段：dev | test（對應 configs/stage-env.js）
STAGE=dev

# 請求逾時（毫秒）
REQUEST_TIMEOUT=10000

# 選填：覆寫目標網域（留空則使用 stage-env 預設）
# OVERRIDE_DOMAIN=
